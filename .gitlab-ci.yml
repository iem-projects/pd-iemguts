image: gcc

build:
  stage: build
  before_script: 
    - apt update && apt -y install make puredata-dev
  script: 
    - export
    - make
    - make install DESTDIR=/tmp/ pkglibdir=iemguts
  dependencies: []
  artifacts:
    paths:
      - /tmp/iemguts/
    expire_in: 1 week
